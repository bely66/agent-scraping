instructions:
- Open amazon.eg
- Find the login button
- Click it and wait for two seconds
- Find all the text inputs and search for user or email text input (don't use placeholders)
- Fill it with the provided email and click enter
- Wait for two seconds
- Search for the inputs
- find the password input
- Enter 1234 in the password field and click enter
- Wait for 2 seconds
- Find the orders button and click it
- Find all the order objects containing (view order details) button
- Loop over all the order objects and print the information inside them
